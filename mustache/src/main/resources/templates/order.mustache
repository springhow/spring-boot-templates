<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Spring Boot - Mustache</title>
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/tailwindcss/1.8.2/tailwind.css"/>
</head>
<body class="flex items-center justify-center h-screen">
<div class="rounded-lg border shadow-lg p-10 w-3/5">
    <div class="flex flex-row justify-between pb-4">
        <div>
            <h2 class="text-xl font-bold">Order #<span class="text-green-600">{{orderEntry.orderId}}</span></h2>
        </div>
        <div>
            <div class="text-xl font-bold">{{orderEntry.date}}</div>
        </div>
    </div>
    <div class="flex flex-col pb-8">
        <div class="pb-2">
            <h2 class="text-xl font-bold">Delivery Address</h2>
        </div>
        <div>{{orderEntry.account.address.street}}</div>
        <div>{{orderEntry.account.address.city}}</div>
        <div>{{orderEntry.account.address.state}}</div>
        <div>{{orderEntry.account.address.zipCode}}</div>

    </div>

    <table class="table-fixed w-full text-right border rounded">
        <thead class="bg-gray-100">
        <tr>
            <th class="text-left pl-4">Product</th>
            <th>Qty</th>
            <th>Price</th>
            <th class="pr-4">Total</th>
        </tr>
        </thead>
        <tbody>
        {{#orderEntry.items}}
            <tr>
                <td class="pl-4 text-left">{{name}}</td>
                <td>{{ quantity}}</td>
                <td>{{ price}}</td>
                <td class="pr-4">{{ price }} x {{quantity}}</td>
            </tr>
        {{/orderEntry.items}}
        </tbody>
    </table>
    <div class="flex flex-row-reverse p-5">
        <h2 class="font-medium  bg-gray-200 p-2 rounded">
            Grand Total: <span class="text-green-600">{{orderEntry.payment.amount}}</span>
        </h2>
    </div>
    <h2 class="text-xl font-bold">Payment Details</h2>
    <table class="table-fixed text-left w-2/6 border">
        <tr>
            <th class="text-green-600">Card Number</th>
            <td>{{orderEntry.payment.cardNumber}}</td>
        </tr>
        <tr>
            <th class="text-green-600">CVV</th>
            <td>{{orderEntry.payment.cvv}}</td>
        </tr>
        <tr>
            <th class="text-green-600">Expires (MM/YYYY)</th>
            <td>{{orderEntry.payment.month}}/{{orderEntry.payment.year}}</td>
        </tr>
    </table>
</div>
</body>
</html>
